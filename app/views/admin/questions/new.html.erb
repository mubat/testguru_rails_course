<% @title = question_header(@question) %>
<%= link_to '<< ' + t('application.button.back'), admin_test_questions_path(@test), method: :get %>
<%= render 'form', model: [:admin, @question.test, @question] %>