<% @title = t 'tests.index.title' %>
<p><%= t 'application.found_count', count: @tests.count %></p>
<table class="table table-striped">
  <thead class="table-dark">
  <tr>
    <th><%= t 'helpers.label.test.category_id' %></th>
    <th class="sort-by-title pointer">
      <%= t 'helpers.label.test.title' %>
      <%= octicon 'arrow-up', class: 'text-success hide' %>
      <%= octicon 'arrow-down', class: 'text-success hide' %>
    </th>
    <th><%= t 'helpers.label.test.level' %></th>
    <th><%= t 'tests.index.fields.actions' %></th>
  </tr>
  </thead>
  <tbody>
  <%= render @tests %>
  </tbody>
</table>