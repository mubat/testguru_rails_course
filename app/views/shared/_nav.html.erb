<div class="navbar navbar-light db-light header">
  <div class="navbar-brand header-item logo"><%= link_to 'TestGuru', root_path %></div>
  <div class="navbar-text">
    <% if user_signed_in? %>
      <div class="logout"><%= link_to t('.logout'), destroy_user_session_path, method: :delete %></div>
      <div class="user"><%= t '.welcome', user: current_user.first_name %></div>
    <% else %>
      <div class="logout"><%= link_to t('.login'), new_user_session_path %></div>
      <div class="logout"><%= link_to t('.sign_up'), new_user_registration_path %></div>
    <% end %>
  </div>
</div>