<tr>
  <td>"<%= question.body %>".</td>
  <td><%= button_to('Подробнее', question_path(question), method: :get) %></td>
  <td><%= button_to('Изменить', edit_question_path(question), method: :get) %></td>
  <td><%= button_to('Удалить', question_path(question), data: {:confirm => 'Вы уверены?'}, method: :delete) %></td>
  </tr>

